# Build output
/nexus
/nexus.exe

# Data / runtime
/data/
*.db
*.db-*

# Node / frontend
web/node_modules/
web/*.tsbuildinfo
# web/dist is the real built SPA output and is committed so `go build` works
# from a clean, Node-free checkout (single-binary design). Do not ignore it.

# Editor / OS
.DS_Store
.idea/
.vscode/

# Claude Code local worktrees
.claude/worktrees/

# Brainstorming visual companion scratch
.superpowers/
