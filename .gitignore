# Build output
/nexus
/nexus.exe

# Data / runtime
/data/
*.db
*.db-*

# Node / frontend
web/node_modules/
# Generated SPA build output is ignored, but keep the Foundation placeholder
# so the //go:embed dist target exists and the binary builds from a clean
# checkout. The real React build replaces this in a later sub-project.
web/dist/*
!web/dist/index.html

# Editor / OS
.DS_Store
.idea/
.vscode/

# Claude Code local worktrees
.claude/worktrees/

# Brainstorming visual companion scratch
.superpowers/
